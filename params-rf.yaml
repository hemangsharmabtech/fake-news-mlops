# params-rf.yaml
data:
  sample_size_per_class: 10000
  test_size: 0.2
  random_state: 42

featurize:
  max_features: 5000
  ngram_range: [1, 2]
  stop_words: "english"

train:
  model_type: "random_forest"
  n_estimators: 100
  max_depth: null
  min_samples_split: 2
  min_samples_leaf: 1
  random_state: 42
  n_jobs: -1

evaluate:
  target_names: ["Fake", "True"]